extends ../layouts/main-layout.pug
block variables
  - var pageTitle = title || 'Orders';
  - var pagePath = path || "/orders";

block styles
  link(rel="stylesheet", href="/css/product.css")

block  content
  h1 orders page here...